{
  "nextId": 35,
  "tasks": [
    {
      "id": "9",
      "subject": "Custom Output Style",
      "description": "Create custom output styles for the agent which change the tone and verbiage it choses its responses with. These could be playful (noir, cowboy) or practical (caveman, researcher).",
      "status": "in_progress",
      "metadata": {},
      "blocks": [],
      "blockedBy": [],
      "createdAt": 1778434155072,
      "updatedAt": 1778442175549
    },
    {
      "id": "22",
      "subject": "Fix `navigateTo()` O(distance) synthetic key events",
      "description": "**File:** `editor.ts:278-304`\n\nEvery cursor jump (`w`, `b`, `e`, `gg`, `ge`, `gw` label jumps, `n`/`N` search navigation) emits O(|lineDelta| + targetCol) individual `super.handleInput()` calls. Jumping from line 20, col 60 back to line 0, col 0 emits ~100 synthetic key events.\n\n**Fix:** Lobby for or shim a `setCursor()` API on `CustomEditor` to replace the synthetic-keypress loop.\n\n**Plan file:** plans/helix-perf-01-navigate-to.md",
      "status": "pending",
      "activeForm": "Fixing navigateTo() performance",
      "metadata": {},
      "blocks": [],
      "blockedBy": [],
      "createdAt": 1778547962137,
      "updatedAt": 1778547962137
    },
    {
      "id": "24",
      "subject": "Cache word-wrap results in `computeSelectionSpans()`",
      "description": "**File:** `label-overlay.ts:175-222`\n\n`computeSelectionSpans()` recomputes word-wrap for ALL lines even when selection covers only one line. Duplicates work already done by `super.render(width)`. No caching or sharing of layout results.\n\n**Fix:** Cache word-wrap results per-render (keyed on text + width), or compute a cumulative visual-row index once and reuse across `computeSelectionSpans` and `buildLabelMap`.\n\n**Plan file:** plans/helix-perf-03-duplicated-word-wrap.md",
      "status": "pending",
      "activeForm": "Caching word-wrap results",
      "metadata": {},
      "blocks": [],
      "blockedBy": [],
      "createdAt": 1778547962139,
      "updatedAt": 1778547962139
    },
    {
      "id": "25",
      "subject": "Cache search matches in `navigateMatch()` instead of rescanning",
      "description": "**File:** `editor.ts:369-399`\n\n`navigateMatch()` rescans entire text on every `n`/`N` press. In Normal/Select mode no edits occur between navigation presses, so a dirty flag or text-identity check would avoid redundant full-text regex scans.\n\n**Fix:** Cache matches and invalidate only when `getText()` identity/length changes.\n\n**Plan file:** plans/helix-perf-04-navigate-match-rescan.md",
      "status": "pending",
      "activeForm": "Caching search matches",
      "metadata": {},
      "blocks": [],
      "blockedBy": [],
      "createdAt": 1778547962140,
      "updatedAt": 1778547962140
    },
    {
      "id": "34",
      "subject": "Replace fragile 30% heuristic in `getMaxVisibleLines()`",
      "description": "**File:** `editor.ts:354-358`\n\n`getMaxVisibleLines()` guesses at the editor's visible height using `Math.floor(rows * 0.3)`. If the editor exposes its actual content height, using the real value would be more robust.\n\n**Plan file:** plans/helix-perf-13-max-visible-lines-heuristic.md",
      "status": "pending",
      "activeForm": "Fixing visible lines heuristic",
      "metadata": {},
      "blocks": [],
      "blockedBy": [],
      "createdAt": 1778547962146,
      "updatedAt": 1778547962146
    }
  ]
}